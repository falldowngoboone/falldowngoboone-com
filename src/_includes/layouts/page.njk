---
layout: default
containerClass: c-page o-stack
eleventyComputed:
  headContent: |
    {{ headContent }}
  scriptContent: |
    {{ scriptContent }}
---

<h1>{{ displayTitle | default(title) }}</h1>
{{ content | safe }}