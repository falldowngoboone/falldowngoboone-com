<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {% if title == empty %}{{ metadata.title }}{% else %}{{ title }}{% endif
      %} | {{ metadata.branding }}
    </title>
    <link rel="stylesheet" href="css/global.css" />
    {{ head }}
  </head>

  <body>
    <a href="#main">Skip to main content</a>
    <header>
      <a href="/">falldowngoboone home</a>
      <details>
        <summary>Menu</summary>
        <nav>
          <a href="/">Home</a>
          <a href="/blog">Blog</a> <a href="/about">About</a>
          <a href="/contact">Contact</a>
        </nav>
      </details>
    </header>
    <main id="main">
      {{ content }}
    </main>
    <footer>
      <p>© 2020 Ryan Boone.</p>
      <ul>
        <li>
          <a href="#">Twitter</a>
        </li>
        <li>
          <a href="#">GitHub</a>
        </li>
        <li>
          <a href="#">LinkedIn</a>
        </li>
      </ul>
    </footer>
    {{ script }}
  </body>
</html>
