import React, { Fragment } from "react"
import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    Hi. My name is Ryan. I’m a front-end developer and designer based in Fort Worth, TX.  I currently help make the space that comes from <Styled.a href="https://www.containerstore.com/">The Container Store</Styled.a>.
  </Fragment>
)
